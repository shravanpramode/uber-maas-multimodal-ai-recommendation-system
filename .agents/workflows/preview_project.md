# Preview Project

Follow these steps to start the local development server.

## Step 1: Start Dev Server
```bash
npm run dev -- --port 8080 --host 0.0.0.0
```

Once the server is running, you can access the application at `http://localhost:8080`.
